
import Dashboard from "./Dashboard";

/**
 * Index page that serves as the entry point to the application
 * Renders the Dashboard component
 */
const Index = () => {
  return <Dashboard />;
};

export default Index;
